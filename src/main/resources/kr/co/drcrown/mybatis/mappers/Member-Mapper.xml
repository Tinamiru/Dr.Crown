<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member-Mapper">
	 <sql id="search">	
		<if test="searchType == 'i'.toString()">
			and emp_id like '%'||#{keyword}||'%'			
		</if>
		<if test="searchType == 'p'.toString()">
			and mem_phone like '%'||#{keyword}||'%'			
		</if>
		<if test="searchType == 'e'.toString()">
			and mem_mail like '%'||#{keyword}||'%'			
		</if>
		<if test="searchType == 'n'.toString()">
			and mem_name like '%'||#{keyword}||'%'			
		</if>			
	</sql>	
	
	<select id="selectMemberList" resultType="member">
		select
		* 
		from member
		where emp_id is not null
		<include refid="search" />
		order by mem_logindate desc
	</select>
	<select id="selectMemberListCount" resultType="int">
		select
		count(*) 
		from member
		where emp_id is not null
		<include refid="search" />
	</select>
	<select id="selectMemberById" parameterType="str" resultType="member">
		select
		*
		from member
		where emp_id=#{emp_id}
	</select>
	<select id="selectMemberByPicture" parameterType="str" resultType="member">
		select
		*
		from member
		where mem_pic=#{mem_pic}
	</select>
	
	
	<update id="insertMember" parameterType="member">
		insert into 
		member(emp_id, mem_pwd, mem_mail, mem_phone,mem_pic,mem_name,mem_authority)
		values(#{emp_id},#{mem_pwd},#{mem_mail},#{mem_phone},#{mem_pic},#{mem_name},#{mem_authority})
	</update>
	
	<update id="updateMember" parameterType="member">
		update member 
		set 
		mem_pwd=#{mem_pwd},mem_mail=#{mem_mail},mem_phone=#{mem_phone},mem_name=#{mem_name},
		mem_pic=#{mem_pic},mem_enabled=1
		
		<if test="authority != null">
			,mem_authority=#{mem_authority}
		</if>
		
		where emp_id=#{emp_id}
	</update>
	
	<update id="deleteMember" parameterType="str" >
		delete 
		from member 
		where emp_id=#{emp_id}		
	</update>
	 
	
	
</mapper>










